"use client";

import DashboardPage from "../page";

export default function WebsitesPage() {
  // Render the full dashboard (websites list) at the /dashboard/websites route
  return <DashboardPage />;
}
